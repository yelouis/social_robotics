# Layer 03f (Motor Resonance) — 50-Clip Smell Test

**Date**: June 14, 2026 · **Host**: Mac Studio M4 Max, MPS · Manifest: the standard 50-clip filtered sample (reused from 03d/03e). Runner: `run.py` (YOLOv8x-pose + Farneback). 48 s, 0 crashes, no error log.

## Headline: 0/50 usable signal
| metric | value |
|---|---|
| records | 50 |
| scored | 6 |
| sentinels | **44** (all `no_pose_data`) |
| **`motor_resonance_detected`** | **0/50** |
| **`mirroring_detected`** | **0/50** |
| bystander_pose_velocity_peak | 0.0 on every scored person |
| empathy_scalar | 0.0 on every scored person |
| ego_kinetic_chaos_score | 0.97–1.0 (healthy — wearer jolts are detected) |

03f produces **no motor-resonance and no mirroring on the entire corpus**. The ego-motion (wearer) half works; the bystander-pose half is starved.

## Root cause — the wearer-vs-bystander window mismatch (again)
03f extracts bystander pose only at the **Node-02 detection timestamps that fall inside the strict `task_reaction_window_sec`** (wearer optical-flow climax ±~1 s). But:
- **Median climax→nearest-bystander-detection offset = 10.1 s** (max 112.8 s); **41/50 clips** have their nearest detection > 1 s from the climax → the window holds **0** bystander detections → `no_pose_data` (44 clips).
- The ≤ 7 clips with an in-window detection get only **1** (Node-02's ~6 s cadence vs a 2 s window), and `bystander_pose_velocity_peak` needs **≥ 2** consecutive keypoint frames → velocity 0.0 (6 "scored" clips).

This is the **same anchoring/geometry family** that 03b (Resolved #2), 03d (Resolved #1), and 03e (Resolved #4, this session) all fixed — 03f never received it. Both manifestations trace to one fact: a 2 s wearer-climax window vs ~6 s-cadenced, ~10 s-offset bystander detections.

## Spot-check
- `044a7a23` (chaos 0.97, 1 in-window detection, velocity 0.0): the t = 192 s frame shows a **large, close bystander plainly present** during the wearer's jolt — but a single in-window detection yields no velocity. Illustrates that the bystander side isn't absent, it's *under-sampled*. (`frames/044a7a23_bystander.jpg`)
- No positive exemplars exist (0 resonance / 0 mirroring), so there is nothing to visually confirm on the detection side.

## Filed
docs/03f **Unresolved Issue 1** (this was previously "None at this time"), with three options + a blank selection line:
- **A (recommended)**: dense in-window pose sampling — decode the reaction window at a fixed `TARGET_POSE_FPS` on the bystander's interpolated bbox (decoupled from Node-02's sparse detections), keeping the climax-centered window so the resonance premise holds.
- **B**: widen the pose-pairing window around the climax.
- **C**: accept as an honest modality limit.

## Verdict
- 03f's **mechanics are sound** (ego-chaos healthy, sentinels honest, 0 crashes, Resolved #1–#7 hold).
- But on this corpus it yields **nothing** — the bystander-pose extraction is starved by the strict wearer-climax window + sparse detection cadence, exactly the class of bug already fixed in 03b/03d/03e. Documented for selection; the recommended fix (dense in-window pose sampling) keeps the resonance premise intact.

## Artifacts
`manifest_03f_50.json`, `03f_result_50.json`, `run.log`, `supervise.log`, `analyze.py`, `frames/044a7a23_*`.
